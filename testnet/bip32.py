import os
import hmac
import hashlib
import ecdsa
import struct
import codecs

def ckd():
    signature = hmac.new(secret, prepped.body, digestmod=hashlib.sha512)


# def generateMainKey(seed):


def hmacsha512(text, key):
    if type(text) == string
    plianText = bytearray(text)
    sign = hmac.new(key, text, digestmod=hashlib.sha512)
    return sign.digest

def main():
   
    


if __name__ == "__main__":
    main()